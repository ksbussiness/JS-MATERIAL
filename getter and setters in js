
Getters and setters are special methods that let you define how to access and update a property — while still behaving like normal property access from the outside.

They’re most often used in objects and classes.

let obj={
    _name:"sachin",

get name(){
    return this._name;
},

set name(value){
    this._name=value.toUpperCase();
}
};

console.log(obj.name) // calls the getter;
obj.name="sai" // calls the setter;
console.log(obj.name) //



example:-2
------------------ >

class Circle {
    constructor(radius){
        this._radius=radius;
    }
    
    
    get area(){
        return Math.PI * this._radius**2;
    }
    
    set radius(value){
        if(value<=0) throw new Error("Radius must be positive");
        this._radius=value;
    }
    
}

let c= new Circle(5);
console.log(c.area);// calls the getter 78.53981633974483
c.radius=10; // calls the setter
console.log(c.area) // 314.1592653589793

